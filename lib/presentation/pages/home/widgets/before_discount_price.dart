import 'package:flutter/material.dart';

import '../../widgets/custom_small_subtitle.dart';

class BeforDiscountPrice extends StatelessWidget {
  const BeforDiscountPrice({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const CustomSmallSubtitle(subtitle: '\$534,33');
  }
}
